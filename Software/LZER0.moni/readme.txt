The SBC running the LZER0 node for monitoring needs:
- a Linux user called lzer0
- a bin dir included into the lzer0 home with the content of this bin dir. The dir path must be included in the system path.
- a version of RTKLIB installed
- a crontab that runs all the script 
